# Student-pass-fail-prediction-ML
A deep learning classification project that predicts whether a student passes or fails based on demographic, social, and academic features. The project includes preprocessing, baseline model comparison, hyperparameter tuning, regularisation, and 5-fold stratified cross-validation using TensorFlow, Keras, and scikit-learn.
